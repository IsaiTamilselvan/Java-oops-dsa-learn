class Solution {
    int missingNum(int arr[]) {
        // code here
        int left=arr[0];
        int missing=0;
        for(int i=0;i<arr.length;i++){
            if(arr[i]!=left){
                missing=left;
                break;
            }
            left++;
        }
        return missing;
        
    }
}
